# Fashion-MNIST-Classification-with-CNN

This project implements a deep learning model to classify images from the Fashion MNIST dataset. It demonstrates the effectiveness of Convolutional Neural Networks (CNNs) for image recognition tasks compared to standard dense networks.
